using %PROJECT_NAME%.Attributes;

namespace %PROJECT_NAME%.Configs.Exception
{
    public sealed class AppException : System.Exception
    {
        public readonly ErrorCode code;

        public AppException(ErrorCode code) : base(code.GetStringValue())
        {
            this.code = code;
        }

    }
}